[develop]
vagrant-local ansible_ssh_host=127.0.0.1 ansible_ssh_port=2222 ansible_ssh_user='vagrant' ansible_ssh_private_key_file='.vagrant/machines/default/virtualbox/private_key'

[test]

[accept]

[production]

# groups

# all servers
[all:children]
develop
test
accept
production

# remote servers
[remote:children]
test
accept
production

# remote staging servers
[staging:children]
test
accept