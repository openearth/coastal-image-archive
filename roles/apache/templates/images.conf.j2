# this file is to configure access to the /data directory

alias /sites {{ dim_data_directory }}

<LocationMatch "^/sites+$">
    Options +Indexes
    ErrorDocument 403 /.noindex.html
</LocationMatch>

<Directory {{ dim_data_directory }}>
    Options Indexes FollowSymLinks
    AllowOverride None
    Require all granted
</Directory>
